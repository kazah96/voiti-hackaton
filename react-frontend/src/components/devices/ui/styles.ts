import { makeStyles } from 'shared/theme';

export const useStyles = makeStyles(() => ({
  container: {
    marginTop: 60,
  },
  notDevice: {
    textAlign: 'center',
    fontSize: 30,
  },
  actionButton: {
    cursor: 'pointer',
  },
}));
