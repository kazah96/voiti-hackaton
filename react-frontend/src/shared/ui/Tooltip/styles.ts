import { makeStyles } from 'shared/theme';

export default makeStyles(() => ({
  tooltip: {
    backgroundColor: '#605E5E',
    borderRadius: 4,
    fontSize: 12,
  },
  arrow: {
    color: '#605E5E',
  },
}));
