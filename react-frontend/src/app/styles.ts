import { makeStyles } from 'shared/theme';

export default makeStyles(() => ({
  container: {
    display: 'flex',
    flexDirection: 'column',
    margin: '32px auto',
    fontFamily: 'Roboto',
  },
}));
